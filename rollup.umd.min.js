import { terser } from 'rollup-plugin-terser'
import config from './rollup.base'

export default Object.assign({}, config, {
  output: {
    name: 'SpriteSpin',
    file: 'dist/release/spritespin.min.js',
    format: 'umd',
    sourcemap: true,
  },
  plugins: [
    ...config.plugins,
    terser()
  ]
})
